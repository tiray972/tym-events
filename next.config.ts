import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Route Handler (route.ts) sert le HTML statique directement sur /
  // sans passer par le App Router React
};

export default nextConfig;
